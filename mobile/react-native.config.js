module.exports = {
  // Inter font files — already copied into android/app/src/main/assets/fonts/.
  // Listed here so `npx react-native-asset` re-links them automatically if
  // the Android asset folder is ever wiped (e.g. `android` re-generated).
  assets: ['./src/assets/fonts'],
};
